import fs from "node:fs"
import path from "node:path";

const __filename = import.meta.filename
const __dirname = import.meta.dirname

// const content = fs.readFileSync(path.resolve(__dirname, "files", "short.txt"), {encoding: 'utf-8'})
// console.log(content)
//
// fs.writeFileSync(path.resolve(__dirname, "files", "newShort.txt"), content)

